gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# The one exception: a shared debug key, so every CI build carries the same
# signature and installs over the last one. It signs nothing that matters.
!app/upino-debug.keystore
